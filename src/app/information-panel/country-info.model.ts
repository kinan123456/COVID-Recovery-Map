export class CountryInfo {
    constructor(public name: string, public confirmed: number, 
                public recovered: number, public deaths: number) {
                    
                }
}